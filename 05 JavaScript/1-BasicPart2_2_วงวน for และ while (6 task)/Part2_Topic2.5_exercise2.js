let i = 0 ;
while(++i < 5) console.log(i);


let a = 0 ; 
while(a++ < 5) console.log(a);


// ตอบ ไม่เหมือนกันทั้งหมด 
// Statement แรก แสดงผลเป็น 1 2 3 4 เพราะทำการ +1 ก่อนแล้วถึงเช็คเงื่อนไข
// Statement หลัง แสดงผลเป็น 1 2 3 4 5 เพราะทำการเช็คเงื่อนไขก่อนแล้วค่อยบวก
